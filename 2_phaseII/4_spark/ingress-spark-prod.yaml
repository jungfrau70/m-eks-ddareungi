---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  namespace: spark
  name: spark-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    #nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: letsencrypt-prod-nginx
    ingress.kubernetes.io/ssl-redirect: "true"
    #ingress.kubernetes.io/ssl-redirect: "false"
    kubernetes.io/tls-acme: "true"
spec:
  tls:
  # Replace the placeholder with your domain name (e.g. ingress.dev.blog.shoot.canary.k8s-hana.ondemand.com)
  - hosts:
    - spark.jupyteronline.com
    secretName: nginx-spark-prod-secret
  rules:
    # Replace the placeholder with your domain name (e.g. ingress.dev.blog.shoot.canary.k8s-hana.ondemand.com)
    - host: spark.jupyteronline.com
      http:
        paths:
          - path: /
            backend:
              serviceName: spark-master-svc
              servicePort: 7077
