apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jupyterbook-homepage.fullname" . }}-test-connection"
  labels:
{{ include "jupyterbook-homepage.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jupyterbook-homepage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
